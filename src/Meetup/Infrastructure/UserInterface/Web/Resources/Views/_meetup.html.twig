<a name="meetup-{{ meetup.meetupId }}"></a>
<h2><a href="{{ path("meetup_details", { 'id' : meetup.meetupId }) }}">{{ meetup.name }}</a></h2>
<p>Scheduled for {{ meetup.scheduledFor|date('Y-m-d') }}</p>
<p>{{ meetup.description }}</p>
